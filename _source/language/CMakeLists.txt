PROJECT(Language)

FILE(GLOB_RECURSE HFILE_LIST ${_INCLUDE}/language/*.h)
FILE(GLOB_RECURSE CFILE_LIST ${SOURCE}/ *.c)

INCLUDE_DIRECTORIES(
	${INCLUDE}
	${_INCLUDE}
	.
	)

ADD_LIBRARY(Language ${HFILE_LIST} ${CFILE_LIST})
