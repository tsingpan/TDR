PROJECT(LUA)

FILE(GLOB_RECURSE HFILE_LIST ${_SOURCE}/lua/ *.h)
FILE(GLOB_RECURSE CFILE_LIST ${_SOURCE}/lua/ *.c)

INCLUDE_DIRECTORIES(
	${_SOURCE}/lua/lua-5.2.2/src
	)

ADD_LIBRARY(LUA ${HFILE_LIST} ${CFILE_LIST})
SET_TARGET_PROPERTIES(LUA PROPERTIES OUTPUT_NAME lua52)
