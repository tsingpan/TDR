function on_document_begin(file)

end

function on_document_end()
end

function on_document_error()
end

function on_import(name)
	len = string.len(name)
	prefix = string.sub(name, 1, len - 4)
	suffix = string.sub(name, len - 3, len)
	if(suffix == ".tdr")then
		file = prefix .. '_json_types.h'
	else
		file = name .. '_json_types.h'
	end

	print('#include "' .. file .. '"')
end

function on_typedef(type, arg, newtype)
	print(type, arg, newtype)
end

function on_const(type, real_type, val, base)
	print(type, real_type, val, base)
end

print(9223372036854775808)
print(string.format("%u", 0xffffffffffffffff))
print(string.format("0x%x", -1))
print(string.format("o%o", 8))
print(0xffffffffffffffff)
