PROJECT(DATrie)

FILE(GLOB_RECURSE HFILE_LIST ${INCLUDE}/datrie/*.h)
FILE(GLOB_RECURSE CFILE_LIST ${SOURCE}/ *.c)

INCLUDE_DIRECTORIES(
	${INCLUDE}
	.
	${INCLUDE}/datrie/
	)

ADD_LIBRARY(DATrie ${HFILE_LIST} ${CFILE_LIST})
SET_TARGET_PROPERTIES(DATrie PROPERTIES OUTPUT_NAME datrie)
