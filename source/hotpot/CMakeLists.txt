PROJECT(HotPot)

FILE(GLOB_RECURSE HFILE_LIST ${INCLUDE}/hotpot/*.h)
FILE(GLOB_RECURSE CFILE_LIST ${SOURCE}/ *.c)

INCLUDE_DIRECTORIES(
	${INCLUDE}
	)

ADD_LIBRARY(HotPot ${HFILE_LIST} ${CFILE_LIST})
SET_TARGET_PROPERTIES(HotPot PROPERTIES OUTPUT_NAME hotpot)
INSTALL(TARGETS HotPot ARCHIVE DESTINATION ${LIBRARY_PATH})
